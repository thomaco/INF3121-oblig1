SourceMonitor sample of source code selected from an XML file.

This directory contains a project that is set up to select souce code files for a checkpoint from an XML file. It also
contains a command file that creates a project file that selects code files from the same XML file, along with a
batch file that executes the command file.
.
